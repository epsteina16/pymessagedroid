class Conversation:
	def __init__(self,title,members,messages,id):
		self.title = title
		self.members = members
		self.messages = messages
		self.id = id
	
	def addMessage(message):
		messages.append(message)

	def addMembers(Person):
		members.append(Person)
